apiVersion: v1
kind: Service
metadata:
  labels:
    app: svc-service-registry
  name: svc-service-registry
spec:
  ports:
  - name: 8582-tcp
    port: 8582
    protocol: TCP
    targetPort: 8582
  selector:
    app: svc-service-registry
  sessionAffinity: None
  type: ClusterIP
