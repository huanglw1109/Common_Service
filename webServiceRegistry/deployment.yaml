apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
  labels:
    app: web-service-registry
    app.kubernetes.io/component: web
    app.kubernetes.io/instance: service-registry
    app.kubernetes.io/name: service-registry
    app.openshift.io/runtime: java
  name: web-service-registry
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web-service-registry
  template:
    metadata:
      labels:
        app: web-service-registry
    spec:
      containers:
      - name: web-service-registry
        imagePullPolicy: Always
        image: image-registry.openshift-image-registry.svc:5000/common-service/pgp:c03f797704e02f66bff6bd202f05af70ff0bfc6c
        ports:
        - containerPort: 8582
          protocol: TCP
